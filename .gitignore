# Windows shortcuts to the helper scripts on N:
*.lnk

# OS junk
Thumbs.db
desktop.ini
.DS_Store

# never publish study material in this PUBLIC repo (D11)
*.pdf

# 공개 기출 시험지만 예외 (D21 · 2026-09-12)
!gichul/pdf/*.pdf

# personal plan data must not live in this PUBLIC repo
timetable/plan_*.json

# 교재 본문이 든 데이터는 비공개 studyplandata 로 간다 (2026-08-31)
minbeop/data/*.json
