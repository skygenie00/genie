# bio — 생물 서재 앱

아직 앱이 올라오지 않은 자리입니다. `index.html` 을 이 폴더에 넣으면
`https://skygenie00.github.io/genie/bio/` 로 열립니다.

⚠ 이 저장소는 **공개(Public)** 입니다. 교재 PDF·스캔본은 올리지 않습니다 (D11).
앱 코드와 앱이 쓰는 데이터 파일만 둡니다.

올리는 법 — `N:\개인\claude\g_push.cmd` 를 실행합니다.
고치기 전에는 `g_pull.cmd` 를 먼저 실행합니다.
